@page
@model IndexModel
@{
    ViewData["Title"] = "Главная";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8 text-center mb-5">
            <h1 class="display-4 mb-3">Анализатор кода</h1>
            <p class="lead text-muted">
                Анализируйте ваш код на C#, C++ и Java с помощью современных метрик качества
            </p>
        </div>
    </div>

    <div class="row justify-content-center">
        <div class="col-md-8">
            <form method="post" enctype="multipart/form-data" class="mb-5">
                <div class="drop-zone">
                    <input type="file" name="UploadedFile" class="file-input" accept=".cs,.cpp,.h,.hpp,.java" required>
                    <div class="drop-zone-content">
                        <i class="bi bi-cloud-upload display-4 mb-3"></i>
                        <p class="drop-zone-text">Перетащите файл сюда или нажмите для выбора</p>
                        <p class="text-muted small">Поддерживаемые форматы: .cs, .cpp, .h, .hpp, .java</p>
                    </div>
                </div>
                <div class="text-center mt-3">
                    <button type="submit" class="btn btn-primary btn-lg">
                        <i class="bi bi-search me-2"></i>Анализировать
                    </button>
                </div>
            </form>
        </div>
    </div>

    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card mb-4">
                <div class="card-header">
                    <h5 class="card-title mb-0">Поддерживаемые языки</h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-4">
                            <div class="language-card">
                                <i class="bi bi-filetype-cs display-4 mb-3"></i>
                                <h5>C#</h5>
                                <p class="text-muted mb-0">Анализ файлов .cs</p>
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="language-card">
                                <i class="bi bi-filetype-cpp display-4 mb-3"></i>
                                <h5>C++</h5>
                                <p class="text-muted mb-0">Анализ файлов .cpp, .h, .hpp</p>
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="language-card">
                                <i class="bi bi-filetype-java display-4 mb-3"></i>
                                <h5>Java</h5>
                                <p class="text-muted mb-0">Анализ файлов .java</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="card">
                <div class="card-header">
                    <h5 class="card-title mb-0">Метрики анализа</h5>
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-md-4">
                            <div class="metric-info">
                                <h6 class="mb-2">Метрики Холстеда</h6>
                                <p class="text-muted small mb-0">
                                    Оценка сложности программы на основе количества операторов и операндов
                                </p>
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="metric-info">
                                <h6 class="mb-2">Метрики Чепина</h6>
                                <p class="text-muted small mb-0">
                                    Анализ сложности программы на основе типов используемых переменных
                                </p>
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="metric-info">
                                <h6 class="mb-2">Метрики Джилба</h6>
                                <p class="text-muted small mb-0">
                                    Оценка поддерживаемости и качества кода
                                </p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/site.js"></script>
}

<style>
    .drop-zone {
        border: 2px dashed #ccc;
        border-radius: 4px;
        padding: 20px;
        text-align: center;
        cursor: pointer;
        position: relative;
        transition: all 0.3s ease;
    }

    .drop-zone:hover {
        border-color: #666;
    }

    .drop-zone.highlight {
        border-color: #007bff;
        background-color: rgba(0, 123, 255, 0.1);
    }

    .drop-zone input[type="file"] {
        position: absolute;
        width: 100%;
        height: 100%;
        top: 0;
        left: 0;
        opacity: 0;
        cursor: pointer;
    }

    .drop-zone-text {
        color: #666;
        font-size: 1.1em;
    }

    .drop-zone.is-invalid {
        border-color: #dc3545;
    }

    .language-card {
        text-align: center;
        padding: 20px;
        border-radius: 4px;
        background-color: #f8f9fa;
        transition: all 0.3s ease;
    }

    .language-card:hover {
        background-color: #e9ecef;
    }
</style>
